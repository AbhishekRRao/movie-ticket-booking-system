package enums;

/**
 * Enum representing the available payment methods.
 * SRP: Only responsible for payment method states.
 */
public enum PaymentMethod {
    CREDIT_CARD,
    DEBIT_CARD,
    UPI,
    NET_BANKING,
    WALLET
}
