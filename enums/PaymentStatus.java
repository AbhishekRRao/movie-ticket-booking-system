package enums;

/**
 * Enum representing the status of a payment.
 * SRP: Only responsible for payment status states.
 */
public enum PaymentStatus {
    SUCCESS,
    FAILED,
    PENDING,
    REFUNDED
}
